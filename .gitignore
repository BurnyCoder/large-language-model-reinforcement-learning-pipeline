# Saved models
Qwen*/
__pycache__/

# Ignore checkpoints in runs folder (but keep logs and metrics)
runs/**/checkpoint-*/

# HuggingFace cache (models and datasets)
cache/

# Environment variables (contains API keys)
.env

# Experiment tracking (local cache)
.neptune/
wandb/
runs